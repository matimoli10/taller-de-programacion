3.- Netflix ha publicado la lista de películas que estarán disponibles durante el mes de
diciembre de 2022. De cada película se conoce: código de película, código de género (1: acción,
2: aventura, 3: drama, 4: suspenso, 5: comedia, 6: bélico, 7: documental y 8: terror) y puntaje
promedio otorgado por las críticas.
Implementar un programa que invoque a módulos para cada uno de los siguientes puntos:
a. Lea los datos de películas, los almacene por orden de llegada y agrupados por código de
género, y retorne en una estructura de datos adecuada. La lectura finaliza cuando se lee el
código de la película -1.
b. Genere y retorne en un vector, para cada género, el código de película con mayor puntaje
obtenido entre todas las críticas, a partir de la estructura generada en a)..
c. Ordene los elementos del vector generado en b) por puntaje utilizando alguno de los dos
métodos vistos en la teoría.
d. Muestre el código de película con mayor puntaje y el código de película con menor puntaje,
del vector obtenido en el punto c).

program ejercicio_3;
type
  rango = 1..8;
  peliculas = record 
    cod_pelicula: integer;
    cod_genero: rango;
    puntaje_criticas: integer;
  end;
  lista = ^nodo;
    nodo = record
      dato : peliculas; 
      sig: lista;
    end;
  pelicula = record 
    codigo: integer;
    mayor_puntaje : integer;
  end;
  vector = array [rango] of pelicula;
procedure cargardatos(var p:peliculas);
  begin 
    writeln('ingrese el codigo de pelicula');
    readln(p.cod_pelicula);
    if (p.cod_pelicula <> -1)then begin 
      writeln('ingrese el genero de pelicula');
      readln(p.cod_genero);
      writeln('ingrese el puntaje promedio otortgado por las criticas');
      readln(p.puntaje_criticas);
    end;
  end;
procedure agregaratras(var l: lista; var ult:lista; p:peliculas );
  var 
    aux:lista;
  begin 
    new(aux);
    aux^.dato := p;
    aux^.sig := nil;
    if(l = nil)then begin 
      l := aux;
    end
    else begin 
      ult^.sig := aux;
    end;
    ult := aux; 
  end;
procedure cargarlista(var l: lista );
  var 
    p:peliculas;
    ult:lista;
  begin 
    cargardatos(p);
    while (p.cod_pelicula <> -1)do begin 
      agregaratras(l,ult,p);
      cargardatos(p);
    end;
  end;
procedure inicializarvector(var v:vector);
  var 
    i : rango;
  begin 
    for i := 1 to 8 do begin 
      v[i].mayor_puntaje := 0;
    end;
  end;
procedure cada_genero (var v:vector; p:peliculas);
  begin 
    if (v[p.cod_genero].mayor_puntaje < p.puntaje_criticas )then begin 
      v[p.cod_genero].mayor_puntaje := p.puntaje_criticas;
      v[p.cod_genero].codigo := p.cod_pelicula;
    end;
  end; 
procedure recorrerlista(l:lista; var v:vector);
  begin 
    while (l <> nil)do begin 
      cada_genero (v,l^.dato);
      l:= l^.sig;
    end;
  end;
procedure insercion (var v:vector);
  var 
    i,j: integer;
    actual: pelicula;
  begin
    for i := 2 to 8 do begin 
      actual := v[i];
      j:= i - 1;
      while(j > 0) and (v[j].mayor_puntaje < actual.mayor_puntaje) do begin 
         v[j+1] := v[j];
         j :=j -1;
      end;
      v[j+1]:= actual;
    end; 
  end;
procedure mayor_y_menor (v:vector);
  begin 
    writeln('el codigo de pelicula con mas puntaje es: ',v[1].codigo,' con un puntaje de ',v[1].mayor_puntaje );
    writeln('el codigo de pelicula con menor puntaje es: ',v[8].codigo,' con un puntaje de ',v[8].mayor_puntaje );
  end;
var 
  l:lista;
  v:vector;
begin 
  l:= nil;
  inicializarvector(v);
  cargarlista(l);
  recorrerlista(l,v);
  insercion(v);
  mayor_y_menor(v);
end.
